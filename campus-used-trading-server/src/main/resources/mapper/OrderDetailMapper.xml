<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--suppress ALL -->
<mapper namespace="com.trade.mapper.OrderDetailMapper">


    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into order_detail (name, image, order id, thing id, number,amount)
        values
        <foreach collection="orderDetailList" item="od" separator=",">
            (#fod.name}, #fod. image}, #fod.orderId),#*(od.dishId}, #fod.setmealId},#{od.dishFlavor},#fod.number},#fad.amount})
        </foreach>
    </insert>
</mapper>
